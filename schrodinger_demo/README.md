Schrödinger interactive demo (Apex Team)

Run (Windows PowerShell):

```powershell
pip install -r ../requirements.txt
streamlit run app.py
```

Open http://localhost:8501 and choose the new app (or use the URL Streamlit prints).
