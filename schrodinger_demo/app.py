import streamlit as st
import numpy as np

st.set_page_config(page_title="Schrödinger — Demo", layout="centered")
st.title("Эрвин Шрёдингер — простой интерактивный демо")
st.markdown("Apex Team presents: интерактивная история про Шрёдингера — объяснение как для ребёнка.")

st.header("Коротко: кто такой Шрёдингер?")
st.write(
    "Эрвин Шрёдингер — очень умный учёный, который придумал важную формулу и мысленный эксперимент про кота, чтобы показать, как странно ведёт себя микромир."
)

st.header("Мысленный эксперимент: кот в коробке")
st.write("Это просто игра в воображение. Ничего реального не вредим — всё для понимания.")

if 'in_box' not in st.session_state:
    st.session_state.in_box = False
    st.session_state.result = None

col1, col2 = st.columns(2)
with col1:
    if st.button('Положить кота в коробку'):
        st.session_state.in_box = True
        st.session_state.result = None
        st.experimental_rerun()
    if st.button('Открыть коробку'):
        if not st.session_state.in_box:
            st.warning('Сначала положи кота в коробку!')
        else:
            # измерение 50/50
            alive = np.random.rand() < 0.5
            st.session_state.result = 'жив' if alive else 'не жив'
            st.session_state.in_box = False
            st.experimental_rerun()
with col2:
    if st.session_state.in_box:
        st.write('📦 Коробка закрыта. Пока не знаем — кот как будто в двух состояниях одновременно.')
        st.image('https://upload.wikimedia.org/wikipedia/commons/thumb/3/3a/Cat03.jpg/320px-Cat03.jpg', width=180)
    elif st.session_state.result is None:
        st.write('Коробка пуста — положи кота и нажми "Открыть коробку".')
    else:
        if st.session_state.result == 'жив':
            st.success('😺 Кот жив! Мы открыли коробку и увидели, что кот в порядке.')
        else:
            st.error('💤 Кот не в порядке (это мысленный эксперимент). Мы видим другой результат.')

st.markdown('---')
st.header('Пояснение для ребёнка (как для 5-летнего)')
st.write(
    '- Представь волшебную монетку, которая до тех пор, пока ты её не посмотрел, может быть и орлом и решкой одновременно.\n'
    '- Шрёдингер сказал: если перенести такие правила на большую вещь (как кот), получится странная история.\n'
    '- Уравнение Шрёдингера — это как карта, которая показывает все возможные варианты волшебной монетки и как они меняются.\n'
)

st.header('Почему это важно?')
st.write(
    'Уравнение помогает учёным предсказывать, как ведут себя атомы и электроны. Это полезно для технологий: лазеры, микросхемы, и сейчас — квантовые компьютеры.'
)

st.markdown('---')
st.write('Хочешь, я добавлю анимацию открытия коробки, больше картинок или мини-игру, где можно многократно измерять и смотреть статистику?')
